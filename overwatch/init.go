package overwatch


import (
	"log"
)

// Bootstrap starts the environment and reads the config file for services
func Bootstrap(){
    log.Println("The gonit is booting. Go is in now control.")
	for {
		
	}	
}